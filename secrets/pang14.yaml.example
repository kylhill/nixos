# Create secrets/pang14.yaml with `sops secrets/pang14.yaml`; do not copy this
# plaintext example to that path and commit it.
user:
  password-hash: REPLACE_IN_SOPS_EDITOR
wireguard:
  private-key: REPLACE_IN_SOPS_EDITOR
  preshared-key: REPLACE_IN_SOPS_EDITOR
